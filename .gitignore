# Local Claude Code settings (per-machine)
.claude/settings.local.json

# Dependencies
node_modules/
bun.lockb.local

# Local state DB (SQLite) — a rebuildable cache from data/seed.json + runs.
# The human-readable audit trail is data/seed.json + the committed digests/.
state/

# Env & secrets
.env
.env.*
*.local

# OS / editor cruft
.DS_Store
*.log

# Runtime logs (launchd / daily runner)
logs/

# Scratch
scratch/
tmp/
