# Local Claude Code settings (per-machine)
.claude/settings.local.json

# Dependencies
node_modules/
bun.lockb.local

# Local state DB (SQLite) — WAL/shm are transient; ajr.db itself is the audit trail
state/*.db-wal
state/*.db-shm

# Env & secrets
.env
.env.*
*.local

# OS / editor cruft
.DS_Store
*.log

# Scratch
scratch/
tmp/
